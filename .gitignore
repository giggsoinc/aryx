# Secrets — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
manifest.secrets.json
.raven/manifest.secrets.json
.raven/.cache/
.raven/audit/
.raven/guard/digest.log
**/.raven/.model-session.json
**/.raven/state/

# Raven framework — never commit (lives outside project)
raven-claude/
raven-claude-guard/
.raven-claude/
.raven-claude-guard/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
.model.env

# Live tech-scope snapshot (Andie/Raven — read + append across sessions)
Current_State_Aryx.md
Current_State_Aryx.html

# Local Raven ops notes (not product docs)
docs/raven_html.md
raven_html.md

# Local marketing / packaging scratch — not part of the product
aryx-website.html
linkedin-promo.md
aryx_full.zip

# Local design scratch + session state — never part of the shipped package
temp_design/
Aryx_OSS_Updates_08Aug/
.raven/session-backups/
.raven/.push-notice-shown
