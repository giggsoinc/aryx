# Secrets — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
manifest.secrets.json
.raven/manifest.secrets.json
.raven/.cache/
.raven/audit/
.raven/guard/digest.log
**/.raven/.model-session.json
**/.raven/state/

# Raven framework — never commit (lives outside project)
raven-claude/
raven-claude-guard/
.raven-claude/
.raven-claude-guard/

# Raven/Claude local engine state — machine-local, never commit
.raven/state/
.raven/backups/
.raven/session-backups/
.raven/.model-session.json
apps/web/.claude/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
.model.env

# Live tech-scope snapshot (Andie/Raven — read + append across sessions)
Current_State_Aryx.md
Current_State_Aryx.html

# Local Raven ops notes (not product docs)
docs/raven_html.md
raven_html.md

# Not open source — never commit
.claude/skills/andie-insights/

# Local marketing / packaging scratch — not part of the product
aryx-website.html
linkedin-promo.md
aryx_full.zip
