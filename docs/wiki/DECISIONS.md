# Decisions — append-only ADR log
<!-- Format: DEC-NNN | date | agent | decision | rationale -->

| ID | Date | Agent | Decision | Rationale |
|----|------|-------|----------|-----------|
| DEC-001 | 2026-06-11 | conductor | G10 adjudication ladder: Claude API + ChatGPT parallel → human escalation | Majority AI vote covers automated cases; human always wins on split; audit trail required for enterprise trust |
| DEC-002 | 2026-06-11 | adjudicator | Review band [0.75, 0.90); LLM band stays [0.90, 0.92); band pairs = NON-merge for the run | G9 sweep: [0.90,0.92) holds 8.7% TP mass (Febrl1) — keep; pairs in [0.60,0.90) were silently lost, now queued. Wrong-merge worse than missed-merge in audited domains. All four thresholds env-tunable (ARYX_ER_*) |
| DEC-003 | 2026-06-11 | adjudicator | LLM failure during adjudication = queue for human, never fail-open or crash the run | Fail-to-human matches G4's fail-closed posture; the pair becomes labeled data either way |
| DEC-004 | 2026-06-11 | action-architect | Action DSL: JSON with guard reusing the rules-engine `when` matcher verbatim; effects v1 = set_attribute, add_relationship, remove_relationship, set_label; Postgres-first, graph never mutated directly | One condition grammar across rules/axioms/actions is a product feature; FalkorDB is a projection (core architecture rule) |
| DEC-005 | 2026-06-11 | action-architect | MCP `act` tool ALWAYS creates a pending execution — agent-initiated mutations never auto-apply, regardless of the action's approval flag | Trust posture worth marketing: every external-agent mutation faces a human in v1 |
| DEC-006 | 2026-06-11 | confidence-smith | Cluster confidence = MIN merge-edge score, clamped [0.5, 0.99]; human edges count 0.99; singletons 0.5 | A chain is as trustworthy as its weakest merge; min not mean. Certainty (1.0) is never claimed |
| DEC-007 | 2026-08-19 | mcp-review | MCP transports (mounted `/mcp` and standalone `:8765`) fail CLOSED on bearer auth; `ARYX_MCP_AUTH=off` is the only opt-out | The tool surface now mutates (ingest, corrections, charts). Both transports previously served unauthenticated callers — the mounted one allowed a missing token and an empty token table, the SSE server had no check — and `/mcp` is exempt from ApiKeyMiddleware, so nothing downstream compensated. Fail-open on a write surface is not a default worth keeping |
| DEC-008 | 2026-08-19 | mcp-review | DEC-005's pending-execution rule is scoped to `act`; `correction_apply` and `chart_confirm` auto-apply | DEC-005's rule named `act`, while its rationale generalised to "every external-agent mutation". Scoping it to `act` for Lite: both other tools are bounded and reversible (a correction is an append-only override, a chart is a spec row), and DEC-007 puts them behind an authenticated transport so the actor is identified. Scope note: statements about agent mutations facing a human apply to `act`. Revisit for multi-tenant or untrusted-agent deployments |
| DEC-009 | 2026-08-19 | andie-jr | Supersedes DEC-002: two-cutoff ER bands — AUTO_MERGE 0.95, REVIEW 0.80, adjudicate band rescores via LLM (see ARCHITECTURE DEC-009) | Spec-driven, not a re-sweep — DEC-002's Febrl TP-mass justification no longer backs the live thresholds |
| DEC-010 | 2026-08-21 | andie-jr | Narrow amendment to DEC-009: inside the adjudicate band, an LLM rescore < `ARYX_ER_AUTO_REJECT` (default 0.05) auto-rejects instead of queuing for human review; new `auto_reject` status, excluded from the human/LLM agreement stat (see ARCHITECTURE DEC-010) | A confident LLM "not a match" (e.g. microsoft.com vs amazon.com scored into the band) was queued identically to a genuine close call, forcing a human to re-confirm a verdict already made. This is a rescore-confidence floor, not a raw-score reject band — pairs that never reach the LLM still queue exactly as DEC-009 specifies |
| DEC-011 | 2026-08-24 | andie-jr | Transactional/fact types skip the resolution funnel entirely — `field_shape.is_row_identifier` fires only when a single proposed key has every sampled value distinct AND code-shaped (order_id, ticket_id); `resolve_entities.materialize_one_per_record` then creates one entity per landed record, no blocking/scoring/adjudication (see ARCHITECTURE DEC-011) | A multi-column fallback (correctly avoiding the id itself) still offered genuinely distinct Orders as merge candidates on partial overlap (same company + status, different product); a human approved one, permanently collapsing several real orders into one entity. Every row of a transactional type is a distinct real-world thing by construction — no similarity heuristic should suggest otherwise. Deliberately narrow: dimension types (Company, a person's own name field) are unaffected |
| DEC-012 | 2026-08-24 | raven-review | Hardens DEC-011 after review found it incomplete: (1) `skip_resolution` now persists in the run's stage-checkpoint detail, so resuming a crashed transactional-type ingest no longer silently falls back to full ER; (2) bypass-created entities get confidence 0.5 (DEC-006's singleton floor) instead of 1.0; (3) the same `resolve_match_keys` classification now runs on every synchronous ingest path (file upload, DB connect, admin single-table, CLI), not file upload only; (4) the chunked resolver (`resolution/chunked.py`, not yet wired into production) gained the same DEC-009/DEC-010 band routing as the in-memory resolver via a shared `_route_decision` helper, instead of silently dropping every non-auto-merge pair (see ARCHITECTURE DEC-012) | Each gap was a correctness hole, not a style nit: as shipped, DEC-011 only protected the false-merge bug it was meant to fix in the happy-path case — a resumed run, a DB-connect/admin/CLI ingest, or a future caller of the chunked resolver would all still be exposed, and the bypass's own confidence value broke DEC-006's "certainty is never claimed" rule |