# Aryx ETL spine dependencies (Increment 1).
# Deterministic stages only — no LLM/graph deps yet (added in later increments).
# Raven CVE gate verifies these at commit; pinned to recent stable releases.
pydantic~=2.9
pydantic-settings~=2.6
psycopg[binary]~=3.2
# AWS Secrets Manager / SSM Parameter Store — used only by AwsSecretProvider.
boto3~=1.35
# Claude API client — frontier/cheap Anthropic model calls via the Broker.
anthropic~=0.40
# FalkorDB client — knowledge-graph projection target (stage 5d).
falkordb~=1.0
# Read API over the graph projection (Increment 6) + ASGI server.
fastapi~=0.115
uvicorn~=0.32
python-multipart~=0.0.17
# Multi-RDBMS ingest: SQLAlchemy introspection + pure-Python dialect drivers.
SQLAlchemy~=2.0
PyMySQL~=1.1
oracledb~=2.4
# Document connectors (Increment 8): PDF, PPTX, DOCX, RTF, image OCR.
pymupdf~=1.24
python-pptx~=1.0
python-docx~=1.1
striprtf~=0.0.26
pytesseract~=0.3.13
Pillow~=11.0
# PII gate (Increment 10): Presidio local analyzer + anonymizer (en_core_web_sm).
presidio-analyzer~=2.2
presidio-anonymizer~=2.2
spacy~=3.7
# pgvector Python adapter (Increment 9) — psycopg3 native vector support.
pgvector~=0.3
# Streamlit UI + graph canvas (Increment 12).
streamlit~=1.41
streamlit-agraph~=0.0.45
# MCP SSE transport (Increment 11).
mcp~=1.6
sse-starlette~=2.2
starlette~=0.41
